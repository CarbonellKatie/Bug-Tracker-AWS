const express = require("express");
const router = express.Router();
const mysql = require("mysql");
const DbService = require("../dbService");

//future routes for functions that require admin permission

module.exports = router;
